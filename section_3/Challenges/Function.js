//1st challenge
/*function calculateTotal(subtotal, tax){
    const total = subtotal + tax;
    return total;
}
console.log(calculateTotal(100, 20));
*/


//2nd challenge
/*function calculateTotal(subtotal, tax){
    const total = subtotal + tax;
    return total;
}
const order1 = calculateTotal(200, 10);
const order2 = calculateTotal(300, 10);
const order3 = calculateTotal(400, 10);
console.log(order1, order2, order3);
*/


//refactor challenge
/*const calculateTotal = function random (subtotal, tax){
    const total = subtotal + tax;
    return total;
}
const order1 = calculateTotal(200, 10);
const order2 = calculateTotal(300, 10);
const order3 = calculateTotal(400, 10);
console.log(order1, order2, order3);
*/
